# 🖼️ Image Compression using Singular Value Decomposition (SVD)

This project demonstrates image compression using Singular Value Decomposition (SVD), a linear algebra technique used in machine learning and data processing.

## 🛠 Tools & Technologies
- Python
- NumPy
- Matplotlib
- OpenCV (optional)

## 🔍 Features
- Convert image into matrix form
- Apply SVD decomposition
- Reconstruct compressed image using reduced singular values
- Compare original and compressed images

## 🚀 How to Run
1. Clone the repository
2. Install dependencies:
##
